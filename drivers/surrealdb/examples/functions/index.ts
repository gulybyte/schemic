import { group } from "../_kit";
import e0 from "./function-with-args-return-type-body";
import e1 from "./function-with-permissions-and-comment";

export const functions = group(
  "functions",
  "DEFINE FUNCTION — args, returns, body, permissions, comment",
  [e0, e1],
);
